apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-api
  template:
    metadata:
      labels:
        app: inventory-api
    spec:
      initContainers:
      - name: sentinel
        image: klight-sentinel:latest
        imagePullPolicy: Never
        env:
        - name: STARTUP_DEPENDENCIES
          value: "postgres:5432 kafka:9092"
        - name: SENTINEL_TIMEOUT
          value: "180"
      containers:
      - name: inventory-api
        image: inventory-api:local
        imagePullPolicy: Never
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: klight-global-config
        - configMapRef:
            name: inventory-api-config
        - secretRef:
            name: klight-global-secrets
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 5
