# Service-specific config for dev environments.
# Keys here override global config (global.env) for this service only.
# No secrets here — use klight-global-secrets or a service-scoped Secret.

# REPLACE_ME_SERVICE_NAME_PORT=8080
# REPLACE_ME_SERVICE_NAME_DB_NAME=replace_me_db
